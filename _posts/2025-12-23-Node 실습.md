---
title: "Node 실습"
date: 2025-12-23 11:00:00 +0900
categories: [ROBOTICS, PiRobot]
tags: [Robotics, ROS2, Node]
description: ""
pin: false
math: true
mermaid: true
hidden: false
---

- 현재 시스템 상에 존재하는 노드들을 확인할 수 있습니다.
    
    ```bash
    ros2 node list
    ```

# Node 프로그래밍
이전에 만들었던 my_package 패키지 내의 my_package 디렉토리에 timer.py 파일을 만들고 다음 코드를 입력합니다.

```python
import rclpy
from rclpy.node import Node

class TimerNode(Node):
    def __init__(self):
        super().__init__('timer_node')
        self.timer = self.create_timer(1.0, self.timer_callback)
        self.count = 0

    def timer_callback(self):
        self.count += 1
        self.get_logger().info(f'Hello ROS2! Count: {self.count}')

def main(args=None):
    rclpy.init(args=args)
    timer_node = TimerNode()
    rclpy.spin(timer_node)
    timer_node.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```
- [RCL (ROS Client Library)](https://docs.ros.org/en/humble/Concepts/Basic/About-Client-Libraries.html)
    - 다양한 프로그래밍 언어로 작성된 노드가 통신할 수 있도록 도와주는 라이브러리
    - ROS2는 다음과 같은 클라이언트 라이브러리를 유지 관리
        - `rclcpp`: C++ 클라이언트 라이브러리
        - `rclpy`: Python 클라이언트 라이브러리
2. `setup.py` 파일을 열고 `entry_points` 섹션을 다음과 같이 수정합니다:
    
    ```python
    entry_points={
        'console_scripts': [
            'timer_node = my_package.timer:main',
        ],
    },
    ```
    
3. 워크스페이스의 루트 디렉토리로 이동하여 패키지를 다시 빌드합니다.
    
    ```bash
    cd ~/ros2_ws
    colcon build --symlink-install --packages-select my_package
    ```
    
4. 새로 빌드한 패키지를 사용할 수 있도록 아래 명령어를 입력하거나 새로운 터미널을 열어줍니다.
    
    ```bash
    source ~/ros2_ws/install/local_setup.zsh
    ```
    
5. 이제 생성한 노드를 실행할 수 있습니다.
    
    ```bash
    ros2 run my_package timer_node
    ```
    ![](/assets/img/HiWonderPi/img/Screenshot%20from%202025-12-23%2011-05-16.png)
    
6. 새로운 터미널에서 결과를 확인합니다.
    
    ```bash
    ros2 node list
    ```
    timer_node가 실행 중이어야 검색된다.
    ![](/assets/img/HiWonderPi/img/Screenshot%20from%202025-12-23%2011-08-06.png)

7. 노드에 대한 정보를 보려면 다음 명령을 사용하면 됩니다.
    
    ```bash
    # ros2 node info <node_name>
    ros2 node info /timer_node
    ```
    ![](/assets/img/HiWonderPi/img/Screenshot%20from%202025-12-23%2011-09-12.png)
    ![](/assets/img/HiWonderPi/img/Screenshot%20from%202025-12-23%2011-19-38.png)